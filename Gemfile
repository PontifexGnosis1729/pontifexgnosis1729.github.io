source "https://rubygems.org"

# GitHub Pages-specific Jekyll environment
gem "github-pages", group: :jekyll_plugins

# Bundler (optional, but recommended)
gem "bundler"

# If you want to run Jekyll locally with GitHub Pages:
gem "webrick" # Required for `bundle exec jekyll serve`
